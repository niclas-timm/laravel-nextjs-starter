// import * as types from "../actionTypes";

const initState = [];

const alert = (
    state = initState,
    action: { type: string; payload: object }
) => {
    return state;
};

export default alert;
